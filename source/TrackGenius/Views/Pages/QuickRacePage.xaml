<Page x:Class="TrackGenius.UI.Views.Pages.QuickRacePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:viewModels="clr-namespace:TrackGenius.UI.ViewModels"
      mc:Ignorable="d"
      Title="Quick Race"
      d:DesignHeight="450" d:DesignWidth="800"
      d:DataContext="{d:DesignInstance Type=viewModels:RacePageViewModel}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <ui:DataGrid Grid.Row="0" HorizontalAlignment="Center"
                     VerticalAlignment="Center"
                     ItemsSource="{Binding RaceDataItems}">
          <ui:DataGrid.RowHeaderTemplate>
              <DataTemplate></DataTemplate>
          </ui:DataGrid.RowHeaderTemplate>
        </ui:DataGrid>
    </Grid>
</Page>
