<ui:FluentWindow x:Class="TrackGenius.UI.DriverCreationForm"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 xmlns:model="clr-namespace:TrackGenius.Model;assembly=TrackGenius.Model"
                 mc:Ignorable="d"
                 Title="Driver Creation"
                 Height="400" Width="300"
                 ExtendsContentIntoTitleBar="True"
                 WindowBackdropType="Mica"
                 WindowCornerPreference="Round"
                 WindowStartupLocation="CenterOwner">
    <Window.DataContext>
        <model:Driver />
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="Driver Creation" />

        <StackPanel Grid.Row="1" Margin="20,10,20,10">
            <StackPanel Orientation="Horizontal">
                <Border BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}" BorderThickness="2">
                    <Image Name="UserFace" Width="80" Height="80" />
                </Border>
                <StackPanel Orientation="Vertical" Margin="10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*" />
                            <ColumnDefinition Width="2*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition />
                            <RowDefinition />
                        </Grid.RowDefinitions>
                        <ui:TextBlock Text="Driver Name" VerticalAlignment="Center" />
                        <ui:TextBox Grid.Column="1" Grid.Row="0"
                                    Margin="4"
                                    Text="{Binding DriverName}" />
                        <ui:TextBlock Grid.Row="1" Grid.Column="0"
                                      Text="Driver Name" VerticalAlignment="Center" />
                        <ui:TextBox Grid.Row="1" Grid.Column="1"
                                    Margin="4"
                                    Text="{Binding DriverName}" />
                    </Grid>
                </StackPanel>
            </StackPanel>
            <Grid Margin="20,10,20,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*" />
                    <ColumnDefinition Width="2*" />
                </Grid.ColumnDefinitions>
                <ui:TextBlock Text="User Number" VerticalAlignment="Center" />
                <ui:TextBox Grid.Column="1"
                            Margin="4"
                            PlaceholderText="User Number" />
            </Grid>
        </StackPanel>
    </Grid>
</ui:FluentWindow>
