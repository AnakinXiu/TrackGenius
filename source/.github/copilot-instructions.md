# Copilot Instructions

## Project Guidelines
- In this codebase, IMessageParser and serial port settings are paired and should be provided together via IProtocol at service start time rather than separately.